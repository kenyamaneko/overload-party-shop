name: CI

# PR の品質ゲート。ブランチ保護の required status check として使う。
# main への push では test-catalog.yaml から呼ばれ、テスト観点カタログの元になる。
on:
  pull_request:
    branches: [main]
  workflow_call:
    outputs:
      code-changed:
        description: "'true' when the run touched code."
        value: ${{ jobs.ci.outputs.code-changed }}

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  # main の run は GitHub Pages への deploy に繋がるため、後続の push で打ち切らない
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  ci:
    uses: kenyamaneko/overload-party-common/.github/workflows/go-service-ci.yaml@main
    with:
      needs-firestore-emulator: true
      codegen-needs-node: true
    secrets: inherit

name: Publish

# api-shop (Go module) と api-shop-npm (Cloudsmith npm) を手動でタグ付け・公開する。
# バージョンを上げるタイミングは人が判断して workflow_dispatch で実行する。
# タグの手動打ちは禁止 — このワークフローがモジュールタグの唯一の発行元。

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Package to publish"
        required: true
        default: "api-shop"
        type: choice
        options:
          - api-shop
          - api-shop-npm
      bump:
        description: "Version bump level"
        required: true
        default: "patch"
        type: choice
        options:
          - patch
          - minor
          - major

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  id-token: write

jobs:
  publish-api-shop:
    if: inputs.target == 'api-shop'
    runs-on: ubicloud-standard-2
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: packages/api-shop/go.mod

      - name: Vet api-shop
        run: go vet ./...
        working-directory: packages/api-shop

      - name: Build api-shop
        run: go build ./...
        working-directory: packages/api-shop

      - name: Determine next tag
        id: tag
        env:
          PREFIX: packages/api-shop/v
          BUMP: ${{ inputs.bump }}
        run: .github/scripts/publish/next-tag.sh

      - name: Tag and push
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: .github/scripts/publish/tag-and-push.sh

  publish-api-shop-npm:
    if: inputs.target == 'api-shop-npm'
    runs-on: ubicloud-standard-2
    timeout-minutes: 20
    env:
      PREFIX: packages/api-shop-npm/v
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Determine next tag
        id: tag
        env:
          BUMP: ${{ inputs.bump }}
        run: .github/scripts/publish/next-tag.sh

      - name: Build & version npm package
        working-directory: packages/api-shop-npm
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: ../../.github/scripts/publish/build-npm.sh

      - name: Publish to Cloudsmith
        uses: kenyamaneko/overload-party-common/.github/actions/publish-to-cloudsmith@main
        with:
          format: npm
          package-path: packages/api-shop-npm

      - name: Tag and push
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: .github/scripts/publish/tag-and-push.sh
